# ─────────────────────────────────────────────────────────────────
# 凭据 —— 绝不入库
# ─────────────────────────────────────────────────────────────────
.env
# 🔴 必须用通配而不是逐条列举。
#    原来只写了 `.env` / `.env.local` / `.env.*.local` 三条，于是
#    **改配置前顺手做的备份 `.env.bak-20260918-110124` 完全没被忽略** ——
#    而它里面有真实的 IR_WORKER_ADMIN_TOKEN。`git check-ignore` 一验就现形。
#    「逐条列举」永远会漏：`.env.old` / `.env.save` / `.env.bak.1` 都想不到。
.env.*
# 模板要显式放回来（否则会被上面那条吃掉）
!.env.example

# ─────────────────────────────────────────────────────────────────
# 运行产物 —— 结果里含明文账号 / 密码 / JWT / API Key
# ─────────────────────────────────────────────────────────────────
results.json
keys.json
accounts.json
*.json

# 过程截图（`--shot` 产物）
*.png
*.jpg
*.jpeg

# ─────────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ─────────────────────────────────────────────────────────────────
# 工作区私有数据（AI 工作目录：记忆 / 临时文件 / 备份）
# 内含本地开发过程记录，不随源码发布
# ─────────────────────────────────────────────────────────────────
.workbuddy-ai/

# ─────────────────────────────────────────────────────────────────
# 编辑器 / 系统
# ─────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
